# external-notifications

Optional adapters that capture Codex or Claude hook stdin and enqueue it
through a user-configured notification backend. Python and queue paths come
from the environment or the private local `notifications.env` generated by the
installer. Missing infrastructure is logged and returns success so tmux title,
attention, and logging features remain independent.
